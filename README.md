This can be used to download a section, or a whole page as pdf.
This can also print the content of a section, or the whole page.

npm install
Use this to install node_modules

npm run start
To run the application